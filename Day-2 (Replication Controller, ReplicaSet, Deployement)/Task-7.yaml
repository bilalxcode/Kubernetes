# Verify that the change has been rolled out to all replicas.

kubectl get pods -l app=v1 -o wide
kubectl describe pod <pod-name>


or 

kubectl get pods -l app=v1 -o=jsonpath="{range .items[*]}{.metadata.name}: {.spec.containers[*].image}{'\n'}{end}"
